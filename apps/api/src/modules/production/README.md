# Produccion

Vacio en esta iteracion. Aqui iran las ordenes de produccion de elaboraciones
intermedias y su trazabilidad.

Punto de enganche: `GET /recipes/:id/scaled?portions=N` ya devuelve la receta
escalada al lote que se quiera producir, usando `scaleRecipe` del motor.
